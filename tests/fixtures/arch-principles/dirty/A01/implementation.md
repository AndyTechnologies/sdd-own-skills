# Implementation: A01-violation-change

VIOLATION(A01): services share a database: two components write the same tables with no API boundary between them

The rest of the change conforms; this single family violates exactly its principle/anti-pattern.
