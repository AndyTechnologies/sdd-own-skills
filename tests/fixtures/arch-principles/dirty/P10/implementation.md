# Implementation: P10-violation-change

VIOLATION(P10): hidden state: the deploy overwrites existing bytes instead of copy-only-if-missing

The rest of the change conforms; this single family violates exactly its principle/anti-pattern.
