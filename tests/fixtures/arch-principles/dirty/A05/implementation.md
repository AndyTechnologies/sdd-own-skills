# Implementation: A05-violation-change

VIOLATION(A05): mutable global state: a singleton store is mutated from multiple call sites and tests cannot isolate it

The rest of the change conforms; this single family violates exactly its principle/anti-pattern.
