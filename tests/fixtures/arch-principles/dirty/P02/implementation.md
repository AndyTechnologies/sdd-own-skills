# Implementation: P02-violation-change

VIOLATION(P02): implicit convention: the contract surface is enforced by prose only, with no byte-pinned header or stable ID scheme

The rest of the change conforms; this single family violates exactly its principle/anti-pattern.
