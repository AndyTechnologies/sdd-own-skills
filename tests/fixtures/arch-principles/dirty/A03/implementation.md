# Implementation: A03-violation-change

VIOLATION(A03): shared DB as integration: two components read and write the same tables with no API boundary

The rest of the change conforms; this single family violates exactly its principle/anti-pattern.
