# Implementation: P01-violation-change

VIOLATION(P01): cross-layer import: orchestration script imports a function from the quest prompt, crossing the boundary

The rest of the change conforms; this single family violates exactly its principle/anti-pattern.
