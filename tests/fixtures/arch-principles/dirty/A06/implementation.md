# Implementation: A06-violation-change

VIOLATION(A06): excessive sync: a deep synchronous call graph with no buffering, timeout or fallback

The rest of the change conforms; this single family violates exactly its principle/anti-pattern.
