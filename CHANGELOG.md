# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased] - 2026-09-08

### Added
- feat(workflow): rename the five own extensions to unprefixed names and adopt the unified quest flow — skills/agents `product-quest`, `architecture-quest`, `architecture-lint`, `rfc-author`, `architecture-plan` (drop the `sdd-` alias); Product Quest now runs ALWAYS after exploration (ODD and SDD), Architecture Quest runs whenever the request involves product/feature work or architectural decisions, `architecture-plan` runs only for substantial/large changes (consuming the approved RFCs), and `architecture-lint` always runs as part of the apply verification, checking the implementation against the generated RFCs (`product-rfc.md` / `arch-rfc.md`) and the acta when one was produced (fail-closed if arch-plan ran but the acta is missing).
- New vendored skill `convert-documents-to-markdown` (firecrawl/anydoc, MIT, main): convert Word/PowerPoint/Excel/OpenDocument/RTF/EPUB/CSV/PDF to GitHub-Flavored Markdown via the anydoc CLI (`npx -y @firecrawl/anydoc`, Node 20+, no install), full-installed to the three agent runtimes by the existing sync mechanics with provenance README.
- New vendored skill `archify` (tt-a1i/archify, MIT, stable v2.16.0): architecture/workflow/sequence/dataflow/lifecycle diagrams as self-contained HTML from typed JSON IR; runtime uses only Node builtins (>=18), full-installed to the three agent runtimes by the existing sync mechanics with provenance README.
- `sdd-verify` now shape-validates evidence claims; malformed evidence is discarded, the unit marked `not-verifiable`, and verify blocks until apply corrects it — no degraded trust from untrusted claims.
- Post-verify Review-Driven Development (RDD) hook in the orchestrator: on RDD ON, a selectorless preflight runs and relays consent as a Lossless Blocking Prompt in both interactive and auto modes, never skipping human authorization; declined consent continues the pipeline to archive.
- Four new test groups (T28–T31) in `tests/run_red_checks.sh` covering contract pins, synthetic SWU probes, sync idempotency, and F4 hook validation.
- New `sdd-council` support phase: 3 lens agents (`sdd-council-arch`, `sdd-council-product`, `sdd-council-risk`) review design+proposal in parallel; convergence proceeds without interrupting the user, real forks are framed for the user to decide (the model never decides forks alone), rounds are capped at 2 then STOP, and the council never relaunches design.
- The council persists an acta (`openspec/changes/{change}/council.md` + Engram mirror `sdd/{change}/council`) with titled decisions, convergence status, and round count.
- The runtime sync merge now propagates root `subagent_depth: 2` (fragment-wins, idempotent, both jq and python merge engines), enabling the orchestrator → council → lens-agent task chain.
- Command overlays (`sdd-continue` SUPPORT-CONDITIONAL, `sdd-ff` item 6) wire the ALWAYS council → arch-lint(acta) chain, and new RED checks T32–T39 pin the council allow-lists, `OWN_PROMPTS` entry, no-`mcp` rule, and `subagent_depth`.
- New `sdd-tool` Go CLI (cobra, pure-Go modernc sqlite, no CGO) closing four SDD gaps plus an incident overlay: `worktree list|verify`, `retro lookup|persist`, on-demand `dashboard` (Bubbletea TUI), and `bug record|resolve|list`, with `--json` on every script surface and ONE shared cached parse of `gentle-ai sdd-status --json` (schema v2) feeding all listing surfaces.
- Hard fail-open execution: tool absence degrades executors to the pre-tool flow; reads warn-and-continue, writes (`retro persist`, `bug record`, `bug resolve`) exit non-zero with a loud FAIL-OPEN marker; `nextRecommended`, `blockedReasons`, and the attempt ledger are never mutated; Engram is reached via the `engram` subprocess only (`~/.engram/engram.db` is never written directly).
- `retro persist` is store-first with cross-store fallback and dedupe by change name: the openspec file is written pre-archive (so the archive folder move carries it) and an Engram observation is saved (topic `sdd/{change}/retrospective`, type `learning`, title carrying the key); the precis is capped at 5 retros of ≤15 lines; `none` mode emits an inline hint only.
- `worktree verify` re-entry check passes only when the three binding signals hold (repo root, `sdd/<change>` branch, scanner parse), names the failing signal, discloses "no worktree" on branch mismatch, reports clean vs. dirty (ignoring server-owned `.codegraph/` and `.sdd-agent-lock`), and never auto-clears — the human decides.
- `dashboard` renders a change table with a detail pane, refreshes only on demand (no polling), emits `--json` consistent with the shared scanner, and is a read-only window (no routing/ledger mutation).
- `bug record|resolve|list` incident recording with privacy-scrubbed summaries (tokens and local paths redacted); `bug resolve --engram-id` binds the fix's direct Engram observation, falls back to `fallback_path` when Engram is off without fabricating an id, and resolved incidents surface in the retro verify-phase incident list.
- Orchestrator archive-close order is now fixed `verify → changelog → retro persist → archive`, with the changelog hook running PRE-archive and consuming the verify-report as its input; four fail-open overlay clauses (prior-context injection, worktree verify rule-5 integration, incident recording hook, archive-close retro persist) are installed in `wiring/prompts/sdd/orchestrator.md`.
- `setup.sh` gains optional step 5d-2 (`go build` of `srv/sdd-tool` honoring check/dry-run/real modes): missing Go or a build failure emits a warning only and never blocks; the binary is gitignored.
- RED checks T40–T48 in `tests/run_red_checks.sh` cover the CLI surface, retro dedupe/fallback/retrievability, worktree verify signals, the incident lifecycle, FAIL-OPEN write markers, and the installed wiring clauses.
- New `git_worktree_acquire` MCP tool: classifies the worktree into one of 7 states, then creates/attaches/re-claims it or confirms it is already yours — returning `{status, path, branch}` with lock v2 and hint-index write-through; active worktrees owned by others are never silently taken over (typed denials `owned_by_other`, `locked_unreadable`, `corrupt_worktree`).
- New `git_worktree_release` MCP tool: removes the caller's claim only — never the worktree, dirty worktrees are fine, and releasing a claim you do not own is a silent no-op.
- 7-state worktree classifier (`absent`, `absent_branch_exists`, `exists_inactive`, `exists_stale`, `exists_active_mine`, `exists_active_other`, `corrupt`) derived from git porcelain + lock file + PID liveness + owner/session match, fail-closed on unreadable evidence.
- Lock v2 schema (version, pid, session, owner, change, repo_root, repo_name, branch, store, created_at, last_seen) with dead-PID stale detection: legacy v1 locks with a dead PID auto re-claim; live-PID v1 locks and same-owner/different-session locks are real conflicts, never silent takeovers.
- Advisory hint index `~/.agent_worktrees/<repo>/.agent-index.json` (version 1, write-through on acquire/release, rebuilt when corrupt or missing; git porcelain stays the source of truth).
- Organic post-power-loss continuation: natural entries such as "continúa con el cambio" discover work via `git_worktree_list` + apply-progress cross-reference with no change name required, presented by volume (1 → direct; 2–5 → selection question; >5 → table with validated input; 0 → informative notice proposing `/sdd-new`).
- Orchestrator acquire gate: every phase needing a worktree calls `git_worktree_acquire` BEFORE `sdd-tool worktree verify`; objective stale evidence auto re-claims without prompting, and a blocking prompt appears only on real conflicts.
- `sdd-tool worktree list|verify` and the server now derive the worktree namespace from the git toplevel basename instead of a hardcoded repo name, fixing the lifecycle in repositories other than the one it was built in.
- New shared catalog `skills/_shared/architecture-principles.md` as the single verifiable principles corpus: P01–P10 (name, definition, concrete evidence, default severity) plus an 11-entry anti-pattern table (A01–A11), consumed by path from the quest, the architecture plan, and the architecture lint — no per-skill copies (T51 single-path, T49 schema).
- `sync-skills.sh` extends its shared copy-only-if-missing loop to the catalog (5 loop sites across real and DRY_RUN paths): an existing installed copy is never overwritten and `sync-skills.sh --check` reports zero desyncs after deployment (T30/T39/T52).
- `sdd-quest` architecture branch reworked into context-driven selection: 8 base context questions (the interview never asks principle-by-principle), a declarative branch table (trigger, loaded principles/anti-patterns, branch questions, early-stop, declared precedence) as the ONLY source of question selection — implicit prose branches and scripted state machines are out — and a stack/technology trigger (yes → enable the technology branch, no → skip it preserving the language-agnostic default, stack-confirmed without a branch → record the driver and early-stop) (T53).
- Architecture quest branches load the applicable principles/anti-patterns from the shared catalog by path; applicability validation happens in the plan with justified N/A, never during the interview.
- Fixed 20-question hard budget for the architecture branch with early-stop within budget and no budget raising; exhaustion produces a consolidation report with explicitly classified gaps (decision | knowledge | blocking), never a silent extension; the Product Quest (budget 50, structure) is untouched.
- `sdd-architecture-lint` v2.1 gains axis 3: stable check IDs P01..P10/A01..A11 resolved from the catalog by path, findings carrying `{id, severity, evidence}`, an independent aggregated verdict `axis_3 pass|fail` (fail only when at least one blocker exists), per-check severity (core-principle violations and detected anti-patterns block; ambiguous evidence warns and never blocks), acta N-A justification suppression with visible justification, and the dual signal (axis 2 unmet mandate + axis 3 blocker) when an applicable principle's direction evidence is contradicted (T35/T55–T58).
- arch-plan.md acta gains a mandatory anchored section `## Principios no verificables` (fail-closed when the literal title is missing or translated): one row per principle with the closed enum `applicable | direction evidence | N-A justified`, mandatory evidence and justification, an axis-2 warning for unjustified N/A, and the section still present with N-A justified rows when no principle applies (T54/T59).
- Deployed runtime state imported as repo canonical: quest v4.0 and lint v2.0 SKILL.md byte-exact (T-pinned), the new orchestrator contract at `wiring/prompts/sdd/orchestrator.md` referenced by file (never inline), and four previously unversioned prompts (`sdd-architecture-plan`, `sdd-hard-gate`, `sdd-hard-verify`, `sdd-pre-experience`) versioned under `wiring/prompts/sdd/` (T31/T32/T34–T36).
- `wiring/opencode.sdd.json` registers the hidden file-based `sdd-architecture-plan` subagent on the orchestrator allow-list with additive merge — personal config keys preserved, SDD fragment keys win only within the SDD surface (T60/T37).
- Rollback-safe import: a single-PR revert to HEAD f9ec832 removes the deltas with zero residual desyncs, the sync gates on `sync-skills.sh --check` before touching installed state, and the deployed runtime remains unaffected by the repo-local import (T05/T47/T47b).
- RED suite extended to 62 checks (T49–T60) with a new `tests/fixtures/arch-principles/` corpus (55 fixtures + `expected.json` covering clean, dirty, multi, warning, N-A-justified, dual-signal, missing-checklist, and translated-anchor states); the suite stays under 2 minutes with no regressions in T01–T48/T42b/T47b.
- Two-quest RFC pre-pass: Product Quest (hard budget 50, explicit product RFC gate) then Architecture Quest (hard budget 20, explicit architecture RFC gate); `needs-changes` re-opens only the affected branch within its remaining budget, and budget exhaustion stops with a report.
- `sdd-rfc-author` now writes two separate artifacts (`product-rfc.md` + `arch-rfc.md`); the orchestrator carries no RFC format contract, and the inline approved Q&A plus the two destination paths is the flow's only content-carrying handoff.
- New `sdd-architecture-plan` phase (post-spec, pre-design): consumes `arch-rfc.md` + explore/research evidence + spec deltas, may launch an optional research lane for pattern evidence, and produces a binding titled-decision acta (`arch-plan.md`) that the user approves before design starts (auto mode records approval without interruption).
- Preflight now asks exactly the three runtime-managed canonical groups (pace, artifact store, PR strategy); worktree confirmation and the `gh-git-mcp` availability check are a separate orchestrator step; the PR/worktree lifecycle never starts past phase 0 without the MCP surface. PR lifecycle: draft PR via MCP surfaces from change start with incremental work-unit commits, marked ready only after the close sequence; merge stays human-only; >400-line forecasts fire the ask-on-risk gate.
- Bootstrap canonical paths (`sdd-tool`, `sdd-rfc-author` prompt, skills) pre-resolved at session start; a mid-phase `command not found` is a bootstrap contract violation, not a retryable error.
- RED suite extended to 57 checks (T49–T55): quest budgets + RFC gates, RFC dual-artifact output, arch-plan acta pins, post-apply lint loop, hard-verify opt-in vs hard gate, pre-archive cumulative changelog, pre-experience persistence, no-`mcp` fragment rule, draft-PR/preflight/gh-git-mcp pins, worktree path pin.
- New `cleanup-sdd-own.sh` safe post-rename cleanup: removes stale sdd-own originals (`~/.config/sdd-own`) and agent symlinks whose name has no canonical counterpart in the repo, ONLY after the real sync installed the successor (`--check`/`--dry-run`/`--yes`, exact-path `rm`, `_shared` and non-sdd-own links never touched). Three-way classification: rename-class (successor canonical + installed → removed), STALE (successor canonical but not yet deployed → abort with "run sync", mutate nothing), and manual orphans with no canonical counterpart (poda-era leftovers such as `sdd-council`/`sdd-hard-gate` — never auto-deleted and never blocking the rename cleanup). `--purge-manual` (explicit opt-in) extends the real mode to those class-c orphans only; structural anomalies (directory without `SKILL.md`, symlink inside sdd-own) are never purged, and STALE still aborts everything even with the flag. Symlink targets are RESOLVED (`readlink -f`) instead of text-matched, so relative spellings that differ per runtime (opencode `../../../sdd-own/...` vs claude `../../../.config/sdd-own/...`) are all detected. Real run applied on the live HOME: removed the 7 stale opencode prompt symlinks (previously invisible to the text pattern) and purged the 8 poda-era orphans. RED suite extended to 71 checks (T61) pinning the contract, purge, STALE+purge, and relative-link detection in sandboxes.

### Changed
- `sdd-tasks` now emits Suggested Work Units (SWUs) with four closed-domain tokens (`start`, `finish`, `verification`, `rollback`), each a command or explicit `N/A` with reason, making commands machine-checkable and never free-form prose.
- `sdd-apply` shape-validates every suggested command before execution; malformed commands are rejected fail-closed with a finding and blocked work unit, never executed or paraphrased.
- `sdd-apply` and `sdd-verify` may only edit files inside authorized edit roots; out-of-root edits produce `blocked(edit_authority_missing)` with two exits (fix into authorized roots or grant authority).
- Architecture review split: the always-council post-design chain is replaced by a pre-design Architecture Plan plus an ALWAYS post-apply architecture lint whose axis 2 verifies the architecture acta title-by-title and fails closed when the acta is missing; lint failure relaunches design at most twice, and a 3rd failure stops with a report; the council is removed from the canonical flow (retained as out-of-band machinery with the orchestrator allow-list excluding it).
- `sdd-architecture-lint` is no longer opt-in or boundary-conditional: it gains axis 2, which verifies each acta decision title-by-title and fails closed when the acta is missing; `N/A` remains valid only for empty/trivial designs.
- Orchestrator flow contract restated node-by-node from the canonical diagram: Product Quest → RFC gate → Architecture Quest → RFC gate → RFC Author → explore ‖ research (parallel) → proposal → spec → Architecture Plan (user gate) → design → tasks → apply → post-apply architecture lint (max 2) → verify → cumulative changelog (pre-archive) → archive; council no longer in the canonical chain.
- Explore and research now run in PARALLEL after the RFC author, both consuming the two RFC artifacts; research never blocks explore, and the flow never forces research when no knowledge gap exists.
- Worktree lifecycle moved to `~/.agent_worktrees/<repo>/<change>` (canonical per user decision 2026-09-14; never `/tmp`), with per-worktree `.codegraph/`, a `sdd/<change>` branch, all phases bound via `--cwd`, and supervised-MCP removal behind a safety check.
- `git_worktree_add` and `git_worktree_remove` are now thin retrocompat wrappers over `git_worktree_acquire`/`git_worktree_release`: the `destructive_flow` two-phase envelope, safe-slug rule, and dirty/owner pre-checks are preserved, with no duplicated classifier/lock logic.
- `git_worktree_list` now returns enriched entries (state, owner, session, last_seen, dirty, main worktree included) instead of paths/branches only.
- The worktree error catalog is extended with `locked_unreadable` and `corrupt_worktree` alongside the existing closed error set.
- The vendored `using-git-worktrees` skill gains an additive section 6c documenting the MCP-native lifecycle, the 7-state model, and typed denials (all pre-6c content preserved).

### Fixed
- Worktree hardcode removal: `srv/sdd-tool/internal/worktree/worktree.go` used to hardcode `"sdd-own-skills"` (~L77) for the worktree namespace and lock `repo_name`; both are now derived from `git rev-parse --show-toplevel` basename, so `worktree list|verify` and acquire work correctly in any repository.

### Removed
- v3 ODD-first migration prunes the ALWAYS council and its deploy surface: command and skill overlays under `overlays/commands/` and `overlays/skills/` are dropped (only `overlays/shared/sdd-phase-common.md` remains), and `wiring/prompts/sdd/orchestrator.md` (now inline in gentle-ai v3) plus the `sdd-council`, `sdd-hard-gate`, `sdd-hard-verify`, and `sdd-pre-experience` prompts are no longer deployed or versioned; the monolithic `sdd-quest` skill splits into `sdd-product-quest` + `sdd-architecture-quest`, and `sdd-changelog` is retained as a pre-archive phase (not a full-install skill).